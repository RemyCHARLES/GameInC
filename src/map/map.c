#include "map.h"
#include <stdlib.h>

void initializeMaze(Cell maze[ROWS][COLS]) {
    // Initialize maze with walls, paths, treasures, and monsters
}

void printMaze(const Cell maze[ROWS][COLS]) {
    // Print the maze to the terminal
}